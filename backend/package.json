{
  "name": "queuesmart-backend",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "start": "node listen.js",
    "test": "jest --coverage",
    "smoke": "node scripts/smoke.js",
    "setup": "node scripts/setup-all.js",
    "verify": "node --check scripts/smoke.js && node --check scripts/setup-all.js && jest --coverage"
  },
  "dependencies": {
    "bcrypt": "^6.0.0",
    "cors": "^2.8.5",
    "dotenv": "^17.4.1",
    "express": "^4.18.2",
    "mysql2": "^3.21.0"
  },
  "devDependencies": {
    "jest": "^30.3.0",
    "supertest": "^7.2.2"
  }
}
